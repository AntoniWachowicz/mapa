# Database
MONGODB_URI=mongodb://localhost:27017/mapa_svelte

# Authentication
# AUTH_SECRET: Min 32 characters, use: openssl rand -base64 32
# AUTH_USERNAME and AUTH_PASSWORD have been removed — accounts are now stored in MongoDB.
# Run scripts/seed-dev.js (dev) or scripts/seed-superadmin.js (production) to create accounts.
AUTH_SECRET=your_jwt_secret_min_32_chars_here

# Optional: Node environment
NODE_ENV=development
